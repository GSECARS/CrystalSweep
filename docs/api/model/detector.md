# Detector

Protocol and concrete implementations for area detector models. Each model handles detector-specific acquisition sequences and file writer plugin configuration via EPICS PVs.

::: crystalsweep.model.detector_model
